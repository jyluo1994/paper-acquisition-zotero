{
  "name": "paper-acquisition-zotero",
  "version": "0.2.6",
  "private": true,
  "description": "Zotero plugin and local service for acquiring PDFs through institutional browser profiles.",
  "type": "commonjs",
  "scripts": {
    "start": "node service/src/server.js",
    "check": "node --check service/src/server.js && node --check scripts/browser-fallback.js && node --check zotero-plugin/bootstrap.js && node --check zotero-plugin/preferences.js",
    "build": "bash scripts/build-zotero-plugin.sh"
  },
  "dependencies": {
    "puppeteer-core": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
